# WEB04 — Issue roadmap and human-aware agent index

Status: **PLANNED** as the sole active task in phase `roadmap-foundation-1` after the transition PR merges.

```sh
make context TASK=WEB04
```

WEB04 turns the reviewed roadmap governance into a live, complete GitHub issue graph, a last-known-good public snapshot, and a compact agent index. It adds bounded per-issue context and visible agent-state tooling before the website consumes roadmap data.

It does not implement the website, change Android/QEMU behaviour, close acceptance gates, or supersede H02A. H02A is paused only for this bounded foundation and must receive a fresh phase-start review afterward.
