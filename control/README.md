# Control contracts

- `schemas/node-enrollment.schema.json` — imported phone enrollment data.
- `openapi.yaml` — Host API (JSON-formatted YAML for stdlib validation).
- `examples/` — non-secret examples.

The MVP uses a replaceable controller-capability authenticator. The resource model is intended to survive replacement with mTLS.
