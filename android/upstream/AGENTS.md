# Upstream metadata instructions

Only T00 or a dedicated upstream-sync task changes lock files. Record immutable commits/tags and update the experiment register. Never silently follow a moving branch during an overnight run.
