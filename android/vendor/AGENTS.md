# Vendored-source instructions

Vendor only pinned source needed to build an adapter. Keep lock files, upstream license, patch series, and generation commands. Do not copy whole upstream UIs when only a headless library/platform layer is needed.
