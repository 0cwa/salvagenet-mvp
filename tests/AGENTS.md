# Cross-project test instructions

Tests provide evidence, not architecture. Never weaken assertions to make an implementation pass. Hardware tests must report `BLOCKED-HARDWARE` when authorization/equipment is missing. Test fixtures contain no live credentials.
