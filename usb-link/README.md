# USB networking MVP+

Blocked until every base item in `docs/roadmap/acceptance-ledger.md` is `PASS`.

Planned roots:

```text
usb-link/protocol/       framed AOA control/data protocol
usb-link/node-linkd/     Linux libusb + TAP/NAT service
android/modules/usb-link Android accessory service + QEMU socket bridge
tests/usb/               attach/reconnect/throughput/failover harness
```
