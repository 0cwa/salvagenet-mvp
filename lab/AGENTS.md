# Laboratory instructions

Everything under `lab/` is disposable test infrastructure. It may use permissive local-only policy and insecure HTTP where explicitly marked, but production code must not depend on laboratory credentials, paths, or assumptions.
