# Host QEMU laboratory instructions

- This lab qualifies profile artifacts and guest initialization independently of
  Android packaging; it cannot replace the physical APK-native QEMU gate.
- Keep downloaded images, overlays, firmware copies, keys, and logs under
  ignored `.local/qemu-lab/`.
- Use only loopback host forwarding. Never commit generated SSH private keys.
