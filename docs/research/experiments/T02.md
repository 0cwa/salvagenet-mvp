# T02 — Podroid-derived QEMU adapter evidence

## JVM evidence

The runtime-qemu unit suite snapshots the normalized Podroid-derived direct-kernel argv and the generic ARM64 UEFI argv. Invariant tests cover native-library launcher ordering, dedicated same-thread spawn/reap, single-instance exclusion, stale Unix socket cleanup, bounded stderr capture, instance-scoped paths, loopback-only recovery SSH, fixed/bounded internal QMP commands, NoCloud token validation, and rejection below profile resource minima.

## Hardware evidence

`BLOCKED-HARDWARE`: no authorized physical ARM64 Android device was available in this task worktree. APK-native executable permissions, 16 KiB native alignment on-device, launcher `PR_SET_PDEATHSIG` behavior, actual Alpine boot, actual AAVMF/Ubuntu boot, and Unix QMP interoperability remain device checks.

## Integration observation

The T02 runtime module compiles and its unit suite passes. The requested app assembly currently stops in the separately owned `control-api` module because `MvpControllerAuthenticator.kt` references a missing `org.nodehost.model.SecretValue`; no T02-owned source is involved in that compiler failure.
