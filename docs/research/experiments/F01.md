# F01 — Canonical artifact and profile resolution

## Status

**IN PROGRESS — post-exit hardening.** Initial implementation head `a343b3dc283d22fe49bbef3caefb6e05f446f4a8` passed the full workflow in Actions run `30533830617`. Focused review then found that bare-file legacy artifact fallback was not restricted to the three pinned Podroid qualification inputs. The branch now requires active manifests for Ubuntu and AAVMF artifacts and includes a regression test; this updated head must pass the full workflow before F01 returns to merge-ready.

No physical gate changes status.

## Phase-start review

- Baseline: `main` at `9e497bd33ad8c8a05e2cda9d0ba2e9ffd7a673f7`.
- Phase 0 validation: Actions run `30524765854`.
- Scope decision: profile loading and artifact-manifest unification remained one cohesive task because both were owned by the Android profile/storage and Host API artifact boundary.
- Evidence class: software and package evidence only; no physical gate may change.

## Profile source comparison

The checked-in JSON and the former Kotlin production mirror agreed on profile IDs, boot modes, artifact IDs, disk sizes, initialization kinds, recovery SSH, and most minimum requirements. The mirror nevertheless omitted three Ubuntu qualification checks present in canonical JSON:

- `virtio-block`;
- `virtio-net`;
- `serial-console`.

The JSON also carries fixed compatibility fields that the mirror did not read from data: architecture `aarch64`, machine family `virt`, acceleration `tcg`, PCI device transport, primary network `slirp`, recovery guest port `22`, recovery bind `loopback`, and raw data-disk format. The packaged parser validates those values instead of silently discarding alternatives.

## Artifact manifest inventory

Before F01, the same active manifest was interpreted independently by:

- controller upload publication and moved-payload recovery;
- public HTTPS import publication, installed checks, and interrupted-publication cleanup;
- Host API image listing;
- QEMU runtime artifact resolution.

Each parser checked a different subset of fields or payload properties. F01 uses one strict adapter requiring the exact versioned fields, digest, size, immutable relative path, payload size, and root containment. Expensive digest verification remains explicit at trust boundaries rather than hidden in every listing call.

## Implemented boundary

- Android assets are generated from the exact checked-in profile JSON, schema, and rendered trusted guest-init sources.
- The APK verifier compares every required profile, schema, index, and guest-init asset byte-for-byte with canonical source output.
- `AndroidPackagedProfileCatalog` reads only the three qualified profiles from bounded immutable APK assets and maps them to the existing typed domain model.
- Fixed Android/QEMU compatibility constraints are validated before mutable disk or QEMU effects.
- `AndroidQemuProfileStorage` no longer constructs complete profiles or maps profile IDs to boot artifacts.
- Guest vendor data is selected through the packaged profile rather than profile-specific Kotlin branching.
- `ArtifactManifestStore` owns active-manifest writing, parsing, listing, payload resolution, installed checks, cleanup decisions, and root containment.
- Controller upload, public HTTPS import, Host API image listing, and QEMU runtime consumption use that adapter.
- Bare-file plus `.sha256` fallback is restricted to `podroid-kernel`, `podroid-initramfs`, and `podroid-alpine-squashfs`; Ubuntu cloud and AAVMF artifacts require active manifests.
- Static CI guards prohibit reintroducing complete profile construction in runtime storage or direct active-manifest interpretation outside the adapter.

## Phase-end searches

The production-boundary check is encoded in `tools/ci/check-profiles.py`:

- `AndroidQemuProfileStorage.kt` contains neither complete `VmProfile(...)` construction nor a profile-ID `when` mirror.
- Runtime storage uses `AndroidPackagedProfileCatalog`.
- No production Kotlin file outside `ArtifactManifest.kt` contains active-manifest filename/version/key literals.
- Upload, HTTPS import, and QEMU storage each reference `ArtifactManifestStore`.
- The application build requires `package-assets.py` and `verifyNodeHostProfilePackaging`.

## Previous validation evidence

Superseded implementation head:

```text
a343b3dc283d22fe49bbef3caefb6e05f446f4a8
```

GitHub Actions run:

```text
30533830617
```

That head passed every workflow stage, including static validation, JVM/domain tests, Android tests/lint, guest qualification, APK construction, exact profile assets, Podroid runtime packaging, signature, 16 KiB alignment, and candidate upload.

Previous candidate evidence:

```text
sourceCommit: a343b3dc283d22fe49bbef3caefb6e05f446f4a8
workflowRunId: 30533830617
apkSha256: ab3d3b841d481088d2f4b8e8800abaa73884a17051484853a8d2060246527928
apkSizeBytes: 348561505
signatureVerified: true
alignment16KiBVerified: true
hardwareValidated: false
workflowArtifactId: 8755952223
workflowArtifactDigest: sha256:73c7ff9a1344f4915f8d90ce4aced5d174da023b49e49f5f7d6098c698c024db
```

The downloaded archive's APK hash matched both `release-candidate-evidence.json` and `nodehost-debug.sha256`.

One earlier run reached `:app:packageDebug` and failed without diagnostic exception text. Re-running the exact failed Android job without a code change passed every stage, and the later implementation head also passed. This remains recorded as a transient runner/package execution failure.

## Post-exit hardening finding

The previous implementation isolated fallback behind `AndroidQemuProfileStorage`, but any artifact ID with a bare file and matching `.sha256` could still bypass an active manifest. That did not meet the stated F01 contract.

The hardened implementation now:

- permits legacy bare-file resolution only for the three pinned Podroid qualification artifacts;
- requires active manifests for `ubuntu-2404-arm64-cloud`, `aavmf-code`, `aavmf-vars`, and every other non-Podroid artifact;
- rejects a bare AAVMF fixture even when its `.sha256` is valid;
- updates runtime test fixtures to publish Ubuntu and AAVMF artifacts through `ArtifactManifestStore`.

## Acceptance progress

- [x] Packaged, build-validated JSON is the sole production profile definition for all three qualified profiles.
- [x] Complete Kotlin profile construction was removed from runtime storage.
- [x] Runtime profile loading is bounded, fail-closed, strict-field, compatibility-validated, and typed.
- [x] The APK contains the exact canonical profiles, schema, index, and required rendered guest-init assets on the previous validated head.
- [x] Active artifact publication, listing, cleanup, installed checks, and runtime consumption use one strict manifest contract.
- [~] Legacy fallback is now restricted to the three pinned Podroid qualification inputs; full revalidation is pending.
- [~] H01 upload and public HTTPS-import invariants require revalidation on the hardened head.
- [~] QEMU, profile, guest, Android, package, signature, and alignment checks require revalidation on the hardened head.
- [x] No physical-device result or base-MVP gate is claimed.

## Next-phase decision

The post-F01 preflight work should be split by failure domain rather than activated as the original broad H02/H03 packets:

1. **Guest boot qualification** — canonical Ubuntu profile, pinned artifacts, UEFI/QMP, NoCloud, key-only loopback SSH, restart, and secret-residue inspection. This is the next active uncertainty.
2. **Guest mesh qualification** — one-use Headscale enrollment, distinct guest identity, tailnet SSH, `tailscaled` restart, Headscale interruption, and recovery. Queue until guest boot qualification passes.
3. **Stateless emulator harness** — deterministic API 36 create/boot/install/execute/collect/destroy. Queue until the guest path is qualified or a lifecycle blocker makes it higher priority.
4. **Emulator lifecycle scenarios** — Activity, Service, Room, enrollment, permission, and API behavior through real ports with release-surface exclusion. Queue behind a proven harness.

Only guest boot qualification should enter the next active DAG. The other three remain queued hypotheses and must receive their own phase-start review before implementation.

## Remaining phase-end evidence

Record the hardened final head, full workflow run, exact candidate APK identity, scope result, and completed acceptance checklist before changing F01 back to merge-ready.

## Scope retained

F01 does not change qcow2 copy-once semantics, QEMU argv, orchestration behavior, controller authentication, mesh behavior, native artifact provenance, process isolation, UI technology, or physical acceptance status.