# F01 — Canonical artifact and profile resolution

## Status

**PASS — merge ready.** Reviewed implementation head `b0dae0581c9d72ae0f7481f7b602573931fbc3a2` passed the complete software/package workflow in Actions run `30548116488`. The downloaded APK matched the commit-bound evidence files exactly. No physical-device evidence or base-gate change is claimed.

## Phase-start review

- Baseline: `main` at `9e497bd33ad8c8a05e2cda9d0ba2e9ffd7a673f7`.
- Phase 0 validation: Actions run `30524765854`.
- Scope decision: profile loading and artifact-manifest unification remained one cohesive task because both are owned by the Android profile/storage and Host API artifact boundary.
- Evidence class: software and package evidence only.

## Profile source comparison

The checked-in JSON and former Kotlin production mirror agreed on profile IDs, boot modes, artifact IDs, disk sizes, initialization kinds, recovery SSH, and most requirements. The mirror omitted three Ubuntu qualification checks present in canonical JSON:

- `virtio-block`;
- `virtio-net`;
- `serial-console`.

The JSON also carries fixed compatibility fields the mirror did not read from data: architecture `aarch64`, machine family `virt`, acceleration `tcg`, PCI device transport, primary network `slirp`, recovery guest port `22`, recovery bind `loopback`, and raw data-disk format. The packaged parser validates those values rather than discarding alternatives.

## Artifact manifest inventory

Before F01, the same active manifest was interpreted independently by:

- controller upload publication and moved-payload recovery;
- public HTTPS import publication, installed checks, and interrupted-publication cleanup;
- Host API image listing;
- QEMU runtime artifact resolution.

F01 selects one strict adapter requiring exact versioned fields, digest, size, immutable relative path, payload size, and root containment. Expensive digest verification remains explicit at trust boundaries.

## Implemented boundary

- Generate Android assets from the exact checked-in profile JSON and guest-init sources.
- Verify the APK contains byte-for-byte canonical profile, schema, index, and required guest-init assets.
- Parse only the three qualified profiles from bounded immutable APK assets into existing typed domain values.
- Validate AArch64, `virt`, TCG, PCI, SLIRP, recovery SSH, and vendor-data path constraints before side effects.
- Use profile-derived artifact IDs and boot type for disk preparation rather than a profile-ID mirror.
- Resolve guest vendor data through the selected profile.
- Use `ArtifactManifestStore` for publication, listing, cleanup, installed checks, and QEMU consumption.
- Keep steady-state bare-file plus `.sha256` resolution only for the three pinned Podroid qualification artifacts.
- Migrate only a complete, digest-verified pre-F01 Ubuntu/AAVMF bare bundle into active manifests; reject isolated bare non-Podroid artifacts.

## PR #7 review findings

The automated review produced valid actionable findings, all addressed on the reviewed implementation head:

- Gradle profile tooling paths are module-relative and do not depend on which settings root includes `node-shell`.
- Runtime vendor-data validation rejects empty, `.` and `..` path segments before `AssetManager` access.
- `ArtifactManifestStore.listActive` ignores invalid stray filenames and a remove-after-list race, while still failing closed for malformed valid manifests and for the explicit active-manifest count invariant.
- Manifest temporary tags are validated before filesystem mutation.
- Disk preparation resolves and verifies each source artifact once per preparation, reuses the typed result, copies with a 1 MiB buffer, and verifies copied bytes while streaming.
- Alpine direct-kernel/legacy-vendor, traversal rejection, exact profile cardinality, invalid manifest filenames, and explicit same-length digest-mismatch preconditions have regression coverage.
- F01 documentation uses consistent guest-init wording.

## Final package evidence

```text
validatedHead: b0dae0581c9d72ae0f7481f7b602573931fbc3a2
workflowRun: 30548116488
workflowArtifactId: 8761969616
workflowArtifactDigest: sha256:6394eaca75c0d50d9d8192f3c6922390425a312be2492b0b18a9390fe71b626b
apkSha256: 1a3a03a9259a8e5a197e946861c14c54952045b874228288f802a63822a96ebb
apkSizeBytes: 348568989
signatureVerified: true
alignment16KiBVerified: true
hardwareValidated: false
```

The downloaded archive's APK hash and size matched both `release-candidate-evidence.json` and `nodehost-debug.sha256`.

Actions run `30548116488` passed:

- repository, task, scope, contract, schema, evidence, status, profile, dependency, release-surface, secret, and TODO validation;
- JVM/domain tests;
- Android adapter tests and lint;
- guest/profile qualification;
- APK construction;
- exact canonical profile/guest-init asset verification;
- Podroid runtime verification;
- APK signature verification;
- 16 KiB alignment verification;
- candidate artifact upload.

## Acceptance result

- [x] Packaged, build-validated JSON is the sole production profile definition for all three qualified profiles.
- [x] Complete Kotlin profile construction was removed from runtime storage.
- [x] Runtime profile loading is bounded, strict-field, compatibility-validated, traversal-safe, fail-closed, and typed.
- [x] The APK packaging path covers the canonical profiles, schema, index, and required guest-init assets.
- [x] Active artifact publication, listing, cleanup, installed checks, and runtime consumption use one strict manifest contract.
- [x] Steady-state bare fallback is Podroid-only; complete verified historical Ubuntu/AAVMF state migrates into active manifests, while isolated bare files fail closed.
- [x] Disk preparation reuses each verified source once per preparation, streams copies with a 1 MiB buffer, and verifies copied-byte digests.
- [x] All valid PR #7 findings are addressed with regression tests.
- [x] Static, JVM, Android/lint, guest, package, signature, alignment, and artifact-upload checks passed.
- [x] No physical-device result or base-MVP gate is claimed.

## Next-phase decision

After F01's final documentation head passes and PR #7 merges, activate only:

1. **Guest boot qualification** — canonical Ubuntu profile, pinned artifacts, UEFI/QMP, NoCloud, key-only loopback SSH, restart, and secret-residue inspection.

Keep queued:

2. **Guest mesh qualification** — Headscale enrollment, guest identity, tailnet SSH, interruption, and recovery.
3. **Stateless emulator harness**.
4. **Emulator lifecycle scenarios** behind a proven harness.

## Scope retained

F01 does not change qcow2 copy-once semantics, QEMU argv, orchestration behavior, controller authentication, mesh behavior, native artifact provenance, process isolation, UI technology, or physical acceptance status.
