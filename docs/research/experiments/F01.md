# F01 — Canonical artifact and profile resolution

## Status

**PASS — merge ready.** Implementation head `71a04acedd11221fbefe2c0fa43984141ec11ed4` passed the complete software/package workflow in Actions run `30543765626`. No physical-device evidence or base-gate change is claimed.

## Phase-start review

- Baseline: `main` at `9e497bd33ad8c8a05e2cda9d0ba2e9ffd7a673f7`.
- Phase 0 validation: Actions run `30524765854`.
- Scope decision: profile loading and artifact-manifest unification remained one cohesive task because both are owned by the Android profile/storage and Host API artifact boundary.
- Evidence class: software and package evidence only.

## Profile source comparison

The checked-in JSON and the former Kotlin production mirror agreed on profile IDs, boot modes, artifact IDs, disk sizes, initialization kinds, recovery SSH, and most minimum requirements. The mirror nevertheless omitted three Ubuntu qualification checks present in canonical JSON:

- `virtio-block`;
- `virtio-net`;
- `serial-console`.

The JSON also carries fixed compatibility fields that the mirror did not read from data: architecture `aarch64`, machine family `virt`, acceleration `tcg`, PCI device transport, primary network `slirp`, recovery guest port `22`, recovery bind `loopback`, and raw data-disk format. The packaged parser validates those values instead of silently discarding alternatives.

## Artifact manifest inventory

Before F01, the same active manifest was interpreted independently by:

- controller upload publication and moved-payload recovery;
- public HTTPS import publication, installed checks, and interrupted-publication cleanup;
- Host API image listing;
- QEMU runtime artifact resolution.

Each parser checked a different subset of fields or payload properties. F01 selects one strict adapter requiring the exact versioned fields, digest, size, immutable relative path, payload size, and root containment. Expensive digest verification remains explicit at trust boundaries rather than hidden in every listing call.

## Selected implementation

- Generate Android assets from the exact checked-in profile JSON and rendered trusted guest-init sources.
- Verify the APK contains byte-for-byte canonical profile, schema, index, and guest-init assets.
- Parse only the three qualified profiles from bounded immutable APK assets and map them to existing typed domain values.
- Validate fixed Android/QEMU compatibility constraints during parsing: AArch64, `virt`, TCG, PCI virtio transport, SLIRP, and loopback recovery SSH on guest port 22.
- Use profile-derived artifact IDs and boot type for disk preparation rather than a profile-ID-to-artifact mirror.
- Resolve guest vendor data through the selected profile instead of profile-specific Kotlin branching.
- Use `ArtifactManifestStore` for publication, listing, cleanup, installed checks, and QEMU consumption.
- Keep steady-state bare-file plus `.sha256` resolution only for the three pinned Podroid qualification artifacts.

## Post-exit hardening and compatibility migration

Focused review found that simply restricting all non-Podroid bare files would make the contract strict but could strand a complete pre-F01 Ubuntu/AAVMF installation. The final behavior is narrower and upgrade-safe:

- `podroid-kernel`, `podroid-initramfs`, and `podroid-alpine-squashfs` retain their explicit legacy storage path;
- an isolated bare `ubuntu-2404-arm64-cloud`, `aavmf-code`, or `aavmf-vars` file is rejected even when its sidecar digest is valid;
- only a complete three-artifact historical Ubuntu/AAVMF bundle, with valid bounded files and matching SHA-256 sidecars, may start migration;
- a durable marker binds interrupted migration recovery;
- every migrated payload is moved to its digest-addressed version path and published through `ArtifactManifestStore` before use;
- steady-state resolution after migration is manifest-only.

The existing vertical integration fixture exercises complete-bundle migration. `AndroidQemuRuntimeBackendTest` separately proves that one isolated bare AAVMF artifact cannot bypass the active-manifest contract.

## Production-boundary searches

Static validation now fails if:

- `AndroidQemuProfileStorage` reconstructs a complete `VmProfile`;
- runtime storage reintroduces a profile-ID `when` mirror;
- an active `.manifest.json` contract is interpreted outside `ArtifactManifestStore`;
- upload, public import, or QEMU storage bypasses the shared adapter;
- APK profile packaging verification is removed.

The final searches found no complete production profile mirror and no direct active-manifest parser outside the selected adapter.

## Final validation evidence

```text
validatedHead: 71a04acedd11221fbefe2c0fa43984141ec11ed4
workflowRun: 30543765626
workflowArtifactId: 8760011060
workflowArtifactDigest: sha256:6f2e873938f7e5dcf536862ec310e151d240bde4b1c363b377e972133aaf82c4
apkSha256: 9724c1ae58299f961c850749f3b14aa1b10a9cbc7f988d9b8b9243352da76da0
apkSizeBytes: 348566113
signatureVerified: true
alignment16KiBVerified: true
hardwareValidated: false
```

The downloaded archive's APK hash and size matched both `release-candidate-evidence.json` and `nodehost-debug.sha256`.

Actions run `30543765626` passed:

- repository, task, scope, contract, schema, evidence, status, profile, dependency, release-surface, secret, and TODO validation;
- JVM/domain tests;
- Android adapter tests and lint;
- guest/profile qualification;
- APK construction;
- exact canonical profile/guest-init asset verification;
- Podroid runtime verification;
- APK signature verification;
- 16 KiB alignment verification;
- candidate artifact upload.

An earlier run reached `:app:packageDebug` and failed without diagnostic exception text. Re-running the exact failed Android job without a code change passed every stage, and later complete heads also passed. It remains recorded as a transient runner/package execution failure, not an acceptance exception.

## Acceptance result

- [x] Packaged, build-validated JSON is the sole production profile definition for all three qualified profiles.
- [x] Complete Kotlin profile construction was removed from runtime storage.
- [x] Runtime profile loading is bounded, fail-closed, strict-field, compatibility-validated, and typed.
- [x] The APK contains the exact canonical profiles, schema, index, and required rendered guest-init assets.
- [x] Active artifact publication, listing, cleanup, installed checks, and runtime consumption use one strict manifest contract.
- [x] Steady-state bare fallback is Podroid-only; complete verified historical Ubuntu/AAVMF state migrates into active manifests, while isolated bare files fail closed.
- [x] H01 upload and public HTTPS-import invariants remain green.
- [x] QEMU, profile, guest, Android, package, signature, and alignment checks are green.
- [x] No physical-device result or base-MVP gate is claimed.

## Next-phase decision

The post-F01 preflight work is split by failure domain rather than activated as the original broad H02/H03 packets:

1. **Guest boot qualification** — canonical Ubuntu profile, pinned artifacts, UEFI/QMP, NoCloud, key-only loopback SSH, restart, and secret-residue inspection. This is the next active uncertainty.
2. **Guest mesh qualification** — one-use Headscale enrollment, distinct guest identity, tailnet SSH, `tailscaled` restart, Headscale interruption, and recovery. Queue until guest boot qualification passes.
3. **Stateless emulator harness** — deterministic API 36 create/boot/install/execute/collect/destroy. Keep queued.
4. **Emulator lifecycle scenarios** — Activity, Service, Room, enrollment, permission, and API behavior through real ports with release-surface exclusion. Queue behind a proven harness.

Only guest boot qualification should enter the next active DAG after F01 merges. The other three remain queued hypotheses and require their own phase-start reviews.

## Scope retained

F01 does not change qcow2 copy-once semantics, QEMU argv, orchestration behavior, controller authentication, mesh behavior, native artifact provenance, process isolation, UI technology, or physical acceptance status.
