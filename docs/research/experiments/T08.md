# T08 experiment notes

This page is owned by task T08. Record only measured results, exact commands,
inputs, outputs, and blockers. Promote durable conclusions to an ADR or the
shared experiment register during integration.

## Final integration refresh

Tested source commit: `7120316e69ad27b47a54108bd92ef49f94161a5a`.

From cleaned Gradle outputs with the generated Tailscale checkout/build removed,
`./gradlew test :app:lintDebug :app:assembleDebug
:mesh-tailscale:assembleRelease` passed. All make JVM/Android/guest targets,
explicit API/contract/QEMU/tool/controller/Headscale Python suites, and the fake
vertical e2e path passed. The e2e hardware continuation exited 77 because no
authorized physical device and controller configuration are available.

Focused tests passed for reconciliation maintenance attempts, disk/data
persistence and graceful stop escalation, enrollment phase recovery, the
deployed K3s qualifier asset, cancel receipt/publication races, readiness and
generation, release/debug mesh URL policy, bounded request-body deadlines, model
URI constraints, and diagnostic redaction. This current focused software
evidence supports B14 and B15 as software PASS gates; it does not claim a live
QEMU/device observation.

The debug APK is 348494941 bytes with SHA-256
`7f9d8b9f483fb527d675c2a711640f13312c0ed1ebf8009dcad837badc6c9eb4`.
Exact verification passed for all 42 Podroid runtime lock entries. APK v2
signature, 16 KiB ZIP alignment, AArch64 identity, and minimum `0x4000` PT_LOAD
alignment passed for QEMU, launcher, SLIRP, bridge, and libgojni.

No authorized physical ARM64 Android device exists. B02, B07-B13, B16, and B17
remain `BLOCKED-HARDWARE`; U01-U04 remain `DEFERRED`, and MVP+ remains blocked by
those physical base gates. Full logs remain ignored and are represented only by
redacted hash/size metadata under `evidence/T08/`.
