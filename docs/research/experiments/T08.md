# T08 experiment notes

This page is owned by task T08. Record only measured results, exact commands,
inputs, outputs, and blockers. Promote durable conclusions to an ADR or the
shared experiment register during integration.

## Results

- Automated JVM/Android module, fake e2e, contract/API/CLI, guest, Room race,
  mesh, TLS, QEMU fixture, lint, debug assembly, and security scans passed; see
  `evidence/T08/qa-summary.txt` and per-gate records.
- Disposable Headscale 0.28.0 passed config/health checks on a LAN-reachable
  endpoint, minted distinct one-use keys without disclosure, and shut down.
- API 36 x86_64 emulator 36.6.11 cold boot exited 139 with Vulkan disabled.
  No authorized physical ARM64 device exists, so all device/VPN/live-guest and
  reboot gates remain `BLOCKED-HARDWARE`.
- The initial B19 run failed because QEMU and launcher were absent, and full
  Gradle aggregation exposed T00 terminal-test and T05 generated-AAR wiring
  failures. This evidence remains in Git history and the QA summary records the
  original result.

## Packaging-fix rerun

From a clean generated state (`app/build` and `vendor/tailscale/build` removed),
full Gradle `test`, app debug lint/assembly, mesh release assembly, and app
release assembly passed. The release build used a temporary test-only keystore
under ignored local evidence storage; it was deleted after the build.

`:app:verifyPodroidPackaging`, `apksigner`, `zipalign -P 16`, unzip/readelf, and
the direct prepare-runtime verifier passed. The debug APK is 348464044 bytes
with SHA-256
`df413b753f6e1effe62a2911c26f0cfddb893af8c4ba591bf16b06850ae4a1a3`.
QEMU, launcher, SLIRP, bridge, libgojni, kernel, initrd, rootfs, EFI, and all 34
locked keymaps are present. All five inspected AArch64 native payloads use
16 KiB-compatible PT_LOAD alignment. Exact size and SHA-256 checks passed for
all 42 Podroid runtime lock entries. B19 is now `PASS`; the T00/T05 software
blockers are closed.

`make validate`, every explicit Python suite, guest checks, and the fake e2e
path passed. The e2e hardware continuation exited 77 because no authorized
device/controller configuration is available. MVP+ report-only remains blocked
solely by unchanged `BLOCKED-HARDWARE` base gates.
