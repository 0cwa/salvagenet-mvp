# T08 experiment notes

This page is owned by task T08. Record only measured results, exact commands,
inputs, outputs, and blockers. Promote durable conclusions to an ADR or the
shared experiment register during integration.

## Final source-seal refresh

Tested source commit: `cdf65b20a8e28abd9b9b930ffec35fb73da76a6d`.

From removed Gradle outputs and a removed generated Tailscale checkout/build,
`./gradlew test :app:lintDebug :app:assembleDebug
:mesh-tailscale:assembleRelease :app:verifyPodroidPackaging` passed. All make
JVM/Android/guest targets, explicit API/contract/QEMU/tool/controller/Headscale
Python suites, and the fake vertical e2e path passed. The e2e hardware
continuation exited 77 because no authorized physical device and controller
configuration are available.

Focused reruns passed for 65 maintenance cycles across the 64-record retention
boundary, graceful replacement of an active old runtime generation, and restart
recovery at every enrollment phase including `VALIDATED_STAGED`. Invalid staged
enrollment recovery also failed closed and cleared the stage.

The debug APK is 357663739 bytes with SHA-256
`af7084df7250e3679668bea2923966275a32fce91ef37efc3dc955c6cfb4ccc7`.
Exact verification passed for all 42 Podroid runtime lock entries. APK v2
signature, 16 KiB ZIP alignment, AArch64 identity, and minimum `0x4000` PT_LOAD
alignment passed for QEMU, launcher, SLIRP, bridge, and libgojni.

No authorized physical ARM64 Android device exists. B02, B07-B13, B16, and B17
remain `BLOCKED-HARDWARE`; U01-U04 remain `DEFERRED`, and MVP+ remains blocked by
those physical base gates. Full logs remain ignored and are represented only by
redacted hash/size metadata under `evidence/T08/`.
