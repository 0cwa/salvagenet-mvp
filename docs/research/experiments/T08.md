# T08 experiment notes

This page is owned by task T08. Record only measured results, exact commands,
inputs, outputs, and blockers. Promote durable conclusions to an ADR or the
shared experiment register during integration.

## Results

- Automated JVM/Android module, fake e2e, contract/API/CLI, guest, Room race,
  mesh, TLS, QEMU fixture, lint, debug assembly, and security scans passed; see
  `evidence/T08/qa-summary.txt` and per-gate records.
- Disposable Headscale 0.28.0 passed config/health checks on a LAN-reachable
  endpoint, minted distinct one-use keys without disclosure, and shut down.
- API 36 x86_64 emulator 36.6.11 cold boot exited 139 with Vulkan disabled.
  No authorized physical ARM64 device exists, so all device/VPN/live-guest and
  reboot gates are `BLOCKED-HARDWARE`.
- B19 is `FAIL`: the debug APK verifies as debug-signed and aligned, but omits
  `libqemu-system-aarch64.so` and `libpodroid-launcher.so`.
- Complete Gradle aggregation exposed T00-owned missing terminal test JUnit
  dependencies and T05-owned local-AAR/release task dependency failures.
