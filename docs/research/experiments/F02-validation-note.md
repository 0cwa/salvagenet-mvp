# F02 validation note

This transition must pass repository/task/reference checks and the complete Android/package workflow before merge. H02A implementation starts only afterward from current `main`.
