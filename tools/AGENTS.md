# Repository tooling instructions

Tooling is part of the development product. Prefer deterministic, idempotent scripts with explicit errors. Never run sudo implicitly, never print secrets, and keep scaffold validation usable without Podroid or Android SDK.
