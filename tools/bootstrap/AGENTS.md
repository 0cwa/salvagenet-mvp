# Bootstrap instructions

- Privileged scripts must require the user to invoke them with sudo explicitly.
- Downloads require pinned URLs and checksums or must be performed by an upstream package manager.
- Scripts are idempotent and preserve existing user configuration.
- Distinguish missing software from missing host authorization.
